{
  "processes": [
    {
      "id": "cronmon",
      "cmd": "deno run --ext=ts --unstable --allow-read --allow-net --import-map=deno.json --lock=lock.json cronmon.ts",
      "autostart": true
    }
  ]
}
